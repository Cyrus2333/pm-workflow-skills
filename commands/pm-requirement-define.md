# pm-requirement-define

把模糊想法、用户问题或零散提需推进为可写 PRD 的产品需求定义。

## 使用方式

1. 调用 `pm-requirement-define`
2. 还原现状事实、用户、场景和影响
3. 识别证据缺口，按需使用研究综合、竞品分析或指标复盘，不默认全部执行
4. 定义问题、目标与非目标
5. 方向未定时比较真实可选方案
6. 划定一级范围并扫描会影响方向或投入的重大约束
7. 输出 `ready`、`needs-decision` 或 `needs-input`

达到 `ready` 后转 `pm-prd-write`。
