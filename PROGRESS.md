# PROGRESS

- 目标：在保持现有 PRD 主结构的前提下，提升模板与示例的具体性，使背景充分时能产出无需口头补充、可研发实现和测试验证的产品方案。
- 顺序：先审查规则 / 模板 / 示例缺口，再优先改示例与模板，最后只补必要的 skill 规则和需求定义交接，完成前向阅读验证。
- 最大风险：把“写具体”误解成增加字段、章节和矩阵，导致文档更长但仍需读者自行拼装行为；本次以完整行为链和独立阅读为判断标准。
- 基线：master@49b4912；工作区初始干净；`bash scripts/validate-skills.sh` 通过 6 个 skill；`git diff --check` 通过。
- 当前进度：任务 0、任务 1 已完成，开始优化 skill 规则与参考资料。

## 缺口审查结论

- skill 规则缺口：已经列出应写维度，但没有规定每个维度必须形成“条件—行为—结果—异常—验收”的完整语义；未禁止用字段标签、ID 引用和“按原逻辑”代替正文。
- 模板结构缺口：3.3 仍是空标签清单，告诉作者填哪些槽位，却没有说明各槽位回答什么、写到何种可实现粒度，也没有把用户可见行为、系统行为和多条件规则分开。
- 示例表达缺口：FR-01 只列输入和枚举，未说明优先级、状态生命周期、页面如何消费和失败后的表现；FR-02 的状态表缺进入条件、点击结果、状态切换和明确优先级，且出现“按原逻辑”；FR-03 缺触发边界、去重、参数定义、上报失败处理和可核验方式。
- 输入边界：无法从背景确认的业务决策应标为待确认，不能由 PRD 作者补造；但示例是虚构规格，可给出一套自洽的已确认规则来示范粒度。
- 优化判断：不新增主章节；主要重写 3.3 的写法约束和完整示例，其他章节只补充支持独立阅读所必需的说明。

- 当前进度：任务 2、任务 3 已完成初版；SKILL.md 已增加完整行为规格约束，模板 3.3 已改为“需求结论—行为说明—条件分支—异常恢复—边界—追溯”，示例 FR-01 至 FR-03 已补充具体状态、操作、结果、异常、埋点去重和验收。

- 前向审查发现的补充问题：示例流程表仍有“沿用兜底 / 沿用原返回逻辑”等隐性模糊表达，已改成具体的无权限、加载失败、业务失败和网络失败表现。
- 其他 pm skills 审查结论：竞品、指标、研究和更新说明 skills 已有证据类型、口径、来源、限制和行动结构；本轮未发现与当前“PRD 具体性 / 可读性”同等级的缺口，因此不扩大改动范围。

## 本轮优化与前向验证

- `pm-prd-write` 新增“结论 → 用户路径 → 页面与信息结构 → 功能行为 → 规则与异常 → 验收”的阅读顺序，要求按用户可感知结果拆分功能，并禁止用短标签、ID 或原型引用替代行为正文。
- PRD 模板补充了章节结论、路径闭环、页面转移和信息结构的写作粒度；3.3 明确每个分支需写进入条件、页面表现、可执行操作、系统结果和下一状态，并保留“模糊写法 → 清晰写法”的示例。
- PRD 示例的 FR-01 至 FR-03 已按完整行为链重写：FR-01 明确状态计算时机、输入、优先级、非圈子活动兼容和异常恢复；FR-02 明确每个状态的文案、按钮、点击结果、失败反馈和状态切换；FR-03 明确事件触发、去重、参数、上报失败和验收方式。
- `pm-requirement-define` 仅增加完整句表达和入口—任务—结果交接要求，没有扩展为页面规格编写器；需求定义模板、检查清单和示例同步体现该边界。
- 共享“产品方案六问”和 README 已明确：实现路径不能只是页面名称串联，PRD 的人类可读性以读者能复述入口、页面变化、分支、异常和验收为最低标准。

前向抽查结论：

- 产品 / 设计可以从示例 2.2、2.3 和 3.3 复述“活动入口 → 详情资格判断 → 报名或等待 → 实时结果 / 失败恢复”的路径，以及资格区和主操作区的页面变化。
- 研发可以定位详情请求、服务端状态计算、客户端消费、报名二次校验、非圈子活动兼容和埋点不阻塞主流程的实现边界。
- 测试可以直接根据 AC-01 至 AC-05 覆盖圈内成员、圈外等待、身份未知、活动关闭 / 已报名、非圈子活动、网络失败、重复点击、事件去重和埋点失败。
- 对 PRD、模板和示例执行了 Markdown 表格列数检查，未发现列数不一致；执行模糊短语扫描，剩余命中均为规则中的反例或禁止项说明，不是需求占位。

## 限制与后续观察

- 本轮优化的是产出约束、模板表达和参考示例，不能替代输入背景中的事实、业务决策、原型和技术约束；输入缺失时 skill 必须标记待确认，而不是自动补造。
- “完整替代产品经理”只能在背景信息充分且决策已确认的范围内成立；真实使用中仍需观察不同类型需求是否出现新的具体性或裁剪问题。
- 未提交、未推送，未同步 `/Users/huangjingye/.codex/skills`，未修改验收脚本或白名单外文件。

## 最终验收记录

- `bash scripts/validate-skills.sh`：`Validated 6 skills successfully.`
- `git diff --check`：通过，无输出。
- 白名单检查：通过；当前变更和新增文件均在允许范围内。
- Markdown 表格列数检查：通过；PRD 模板和示例未发现表头与数据行列数不一致。
- 未执行 Git 提交、推送或本地 `/Users/huangjingye/.codex/skills` 同步。
